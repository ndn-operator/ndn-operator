apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: neighborlinks.named-data.net
spec:
  group: named-data.net
  names:
    categories: []
    kind: NeighborLink
    plural: neighborlinks
    shortNames:
    - nl
    singular: neighborlink
  scope: Namespaced
  versions:
  - additionalPrinterColumns: []
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NeighborLink references a public face of an external NDN network
        properties:
          spec:
            properties:
              network:
                description: Name of the local Network CR this link applies to
                type: string
              uri:
                description: Public URI of the neighbor network (e.g., tcp://host:port)
                type: string
            required:
            - network
            - uri
            type: object
        required:
        - spec
        title: NeighborLink
        type: object
    served: true
    storage: true
    subresources: {}
